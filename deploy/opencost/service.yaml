apiVersion: v1
kind: Service
metadata:
  name: opencost
  namespace: opencost
  labels:
    app: opencost
spec:
  ports:
    - name: http
      port: 9003
      targetPort: 9003
      protocol: TCP
  selector:
    app: opencost
  type: ClusterIP
